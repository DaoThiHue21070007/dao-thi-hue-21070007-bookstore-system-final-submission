<div class="max-w-3xl mx-auto px-4">
  <div class="flex items-center justify-between">
    <h1 class="flex-1 font-bold text-3xl">Placement</h1>

    <div class="flex-wrap lg:flex items-center lg:space-x-3">
      <%= link_to placements_path, class: button_class(theme: "transparent") do %>
      All Placements
      <% end %>
      <%= link_to edit_placement_path(@placement), class: button_class(theme:"primary") do %>
      Edit
      <% end %>
    </div>
  </div>

  <ul class="text-slate-700">
      <li>
        <p class="font-semibold">Name:</p>
        <p><%= @placement.name %></p>
      </li>
      <li>
        <p class="font-semibold">Placement:</p>
        <p><%= @placement.placement %></p>
      </li>
  </ul>
</div>
